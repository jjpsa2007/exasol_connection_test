import { SQLQueriesResponse, SQLQueryColumn, SQLResponse } from './types';
import { IStatement } from './sql-client.interface';
import { ConnectionPool } from './pool/pool';
import { Connection } from './connection';
export declare class Statement implements IStatement {
    private readonly connection;
    private readonly pool;
    private readonly statementHandle;
    private readonly columns;
    constructor(connection: Connection, pool: ConnectionPool<Connection>, statementHandle: number, columns: SQLQueryColumn[]);
    /**
     * @inheritDoc
     */
    close(): Promise<void>;
    /**
     * @inheritDoc
     */
    execute(...args: unknown[]): Promise<SQLResponse<SQLQueriesResponse>>;
}
