export declare class ExaError extends Error {
    additionalData?: Array<string | number>;
    constructor(message?: string, additionalData?: Array<string | number>);
}
export declare class ExaErrorBuilder {
    private readonly tag;
    private _message;
    private readonly mitigations;
    constructor(tag: string);
    message(message: string, ...args: Array<string | number>): ExaErrorBuilder;
    private fillPlaceholders;
    mitigation(mitigation: string, ...args: Array<string | number>): ExaErrorBuilder;
    toString(): string;
    error(additionalData?: Array<string | number>): Error;
}
