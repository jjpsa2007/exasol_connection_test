import { ResultSet } from './types';
export declare class QueryResult {
    private readonly resultSet?;
    constructor(resultSet?: ResultSet | undefined);
    getColumns(): import("./types").SQLQueryColumn[];
    getRows(): {
        [column: string]: string | number | boolean | null;
    }[];
}
