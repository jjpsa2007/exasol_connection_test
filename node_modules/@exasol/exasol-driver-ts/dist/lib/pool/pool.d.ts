import { ILogger } from '../logger/logger';
export interface PoolItem {
    broken?: boolean;
    active?: boolean;
    name: string;
}
export declare class ConnectionPool<T extends PoolItem> {
    private readonly max;
    private readonly logger;
    private readonly pool;
    constructor(max: number, logger: ILogger);
    add(connection: T): Promise<void>;
    getAll(): T[];
    clear(): void;
    release(connection: T): void;
    acquire(): T | undefined;
}
